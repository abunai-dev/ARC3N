import type { ManageabilityOption } from "../../../util/types/Option"
const partialReducible : ManageabilityOption =
{
    "type": "manageability",
    "name": "Partial Reducible",
    "definition": "At least partially reducible",
    "exampleScenarios": [
        "By applying scenario-based mitigation strategies"
    ]
}
export default partialReducible;