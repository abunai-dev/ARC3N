import type { ManageabilityOption } from "../../../util/types/Option"
const irreducible : ManageabilityOption =
{
    "type": "manageability",
    "name": "Irreducible",
    "definition": "Uncertainty cannot be further reduced",
    "exampleScenarios": [
        "Due to its aleatory nature"
    ]
}
export default irreducible;