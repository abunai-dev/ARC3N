import type { ReducibleByADDOption } from "../../../util/types/Option"
const reducible : ReducibleByADDOption =
{
    "type": "reducibleByADD",
    "name": "Reducible",
    "definition": "Uncertainty can be reduced by taking an ADD.",
    "exampleScenarios": [
        "By designing the system in a way that the impact of the uncertainty is (partially) mitigated."
    ]
}
export default reducible;