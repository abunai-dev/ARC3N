import type { ReducibleByADDOption } from "@/util/types/Option"
const notReducible : ReducibleByADDOption =
{
    "type": "reducibleByADD",
    "name": "Not Reducible",
    "definition": "Uncertainty is not resolvable or treatable by taking an ADD.",
    "exampleScenarios": [
        "The behavior of a user with a software system."
    ]
}
export default notReducible;